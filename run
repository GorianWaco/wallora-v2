#!/bin/bash
# Quick development runner for Wallora
# Works even when called via symlink (e.g. wallora-dev)

SCRIPT_PATH="$(readlink -f "$0")"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
cd "$SCRIPT_DIR"
export PYTHONPATH=src
exec python3 -m wallora.main "$@"
